autogenerated = 0 

################
### MONITORS ###
################

monitor="VGA-1",1920x1080,auto,1.6
monitor="LVDS-1",1366x768,auto,auto

###################
### MY PROGRAMS ###
###################


# My programs
# $fileManager = yazi
$menu = wofi --show drun
$terminal = foot
$browser = librewolf

# Colors
$active_gradient_start = rgba(00dd00ff)
$active_gradient_end = rgba(00dd00ff)
$inactive_gradient_start = rgba(666666ff)
$inactive_gradient_end = rgba(666666ff)


#################
### AUTOSTART ###
#################

exec-once = waybar

#############################
### ENVIRONMENT VARIABLES ###
#############################

#env = XCURSOR_SIZE,40
env = HYPRCURSOR_SIZE,40


#####################
### LOOK AND FEEL ###
#####################

general {
    gaps_in = 2
    gaps_out = 2

    border_size = 2

    col.active_border = $active_gradient_start $active_gradient_end
    col.inactive_border = $inactive_gradient_start $inactive_gradient_end

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false
    layout = dwindle
}

decoration {

    dim_inactive = yes
    dim_strength = 0.75

	blur {
		size = 2
		passes = 2
	}
	shadow {
		enabled = no
	}

}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = no
}

dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}


misc {
    force_default_wallpaper = -1 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = false # If true disables the random hyprland logo / anime girl background. :(
}


#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = pl
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1

    sensitivity = 0 # -1.0 - 1.0

    touchpad {
        natural_scroll = false
    }
}


# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
}


###################
### KEYBINDINGS ###
###################

$mainMod = SUPER 

# Basics
bind = $mainMod, return, exec, $terminal
bind = $mainMod, C, killactive,
bind = $mainMod, M, exit,
bind = $mainMod, F, fullscreen
bind = $mainMod, W, togglefloating,
bind = $mainMod, space, exec, $menu
bind = $mainMod SHIFT, return, exec, $browser
bind = $mainMod, P, pseudo, # dwindle

# Move focus with mainMod + vim motions
bind = $mainMod, H, movefocus, l
bind = $mainMod, L, movefocus, r
bind = $mainMod, K, movefocus, u
bind = $mainMod, J, movefocus, d

# Group binds
bind = $mainMod, E, togglegroup,
bind = $mainMod, TAB, changegroupactive
bind = $mainMod SHIFT, TAB, changegroupactive, b
bind = $mainMod, up, moveintogroup, up
bind = $mainMod, right, moveintogroup, right
bind = $mainMod, bottom, moveintogroup, bottom
bind = $mainMod, left, moveintogroup, left
bind = $mainMod, up, moveoutofgroup

# Hyprshot binds
bind = ,PRINT, exec, hyprshot -m window -m active -o ~/Shots
bind = SHIFT, PRINT, exec, hyprshot -m region -o ~/Shots

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Special workspace binds
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl s 10%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl s 10%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

##############################
### WINDOWS AND WORKSPACES ###
##############################

# Ignore maximize requests from apps. You'll probably like this.
windowrule =  match:class .* , suppress_event maximize

# Fix some dragging issues with XWayland
windowrule = match:class ^$,match:title ^$,match:xwayland 1,match:float 1,match:fullscreen 0,match:pin 0,no_focus on

# Fix dimming issues with Godot popups
windowrule = match:class Godot, match:title ^$, no_dim on

debug{
	disable_logs=false
}
ecosystem{
	no_update_news=true
}

group{
	col.border_active = $active_gradient_start $active_gradient_end
	groupbar {
		col.active = $active_gradient_start $active_gradient_end
		col.inactive = $inactive_gradient_start $inactive_gradient_end
		font_size = 0
		height = 0
	}
}
